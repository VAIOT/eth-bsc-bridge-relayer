name: Sonarqube
on:
  push:
    branches:
      - master
      - actions
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: docker://sonarsource/sonar-scanner-cli:latest
        env:
          GITHUB_TOKEN: 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
          SONAR_TOKEN: 970d3852dc9a74d65d2ad52e2dfa6afc39532585
          SONAR_HOST_URL: https://sonarqube.veriori.com
