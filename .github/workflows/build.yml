name: Sonarqube
on:
  push:
    branches:
      - master
      - actions
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: docker://sonarsource/sonar-scanner-cli:latest
        env:
          GITHUB_TOKEN: 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
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}  # cec4b89fca373331bf0d52546d9e1a6835ed7fbe
          SONAR_HOST_URL: https://sonarqube.veriori.xyz
