name: Sonarqube
on:
  push:
    branches:
      - master
      - actions
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: docker://sonarsource/sonar-scanner-cli:latest
        env:
          GITHUB_TOKEN: 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
          SONAR_TOKEN: cec4b89fca373331bf0d52546d9e1a6835ed7fbe
          SONAR_HOST_URL: https://sonarqube.veriori.com
